services:
  web:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    environment:
      PUBLIC_BASE_URL: ${PUBLIC_BASE_URL:-http://localhost:8000}
      ADMIN_SECRET: ${ADMIN_SECRET:-}
    volumes:
      - ./data:/app/data
